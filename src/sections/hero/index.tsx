import React from "react";
import Logo from "../../components/logo/Logo";

const Hero: React.FC = () => {
	return (
		<div className="h-[48.171rem]">
			<Logo dark={false} />
		</div>
	);
};

export default Hero;
